1. res.add(new ArrayList(list)), to be honest I don't know why, but if res.add(list) then we are adding just empty list...
2. 	use list.remove(list.size() - 1) cuz list.remove(nums[i]) gives me out of bound exception, don't know why...